Основные команды:

  python3 -m venv venv # создание виртуального окружения
  source venv/bin/activate # активация
  pip install -r requirements.txt # скачка необходимых библиотек
  docker compose us --build -d # если нужно запустить контейнер
  docker compose down # чтоб выключить
